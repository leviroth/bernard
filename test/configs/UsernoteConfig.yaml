subreddits:
- name: thirdrealm
  default_post_actions: false
  default_comment_actions: false
  rules:
  - name: "Usernote added"
    details: "Testing"
    trigger: [n]
    types: [post, comment]
    remove: false
    actions:
    - action: usernote
      params:
        text: "Believes the concept 'horse' is a concept"
        level: "abusewarn"
