subreddits:
- name: thirdrealm
  default_post_actions: false
  default_comment_actions: false
  rules:
  - name: "Removed"
    details: "Testing"
    trigger: [r]
    types: [post, comment]
    remove: true
